import base from "@openhospi/eslint-config";
import react from "@openhospi/eslint-config/react";

export default [
  ...base,
  ...react,
  {
    rules: {
      // `collections/*` is auto-generated by fumadocs-mdx into .source/
      // and resolved via tsconfig paths — not visible to eslint's resolver
      "import-x/no-unresolved": ["error", { ignore: ["^collections/"] }],
    },
  },
  {
    ignores: [".next/**", "out/**", "build/**", "next-env.d.ts", ".source/**"],
  },
];
