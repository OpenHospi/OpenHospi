// Rate limits (per day)
export const RATE_LIMIT_APPLY = 20;
export const RATE_LIMIT_EXPORT_DATA = 3;
export const RATE_LIMIT_JOIN_SHARE_LINK = 10;

// Rate limits (per minute)
export const RATE_LIMIT_CALENDAR_FEED = 1;

// Hard caps (database-checked, not time-based)
export const MAX_ROOMS_PER_USER = 5;
